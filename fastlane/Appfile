# Fallback default only. Each lane passes the correct per-variant app identifier
# explicitly (see fastlane/lib/zodl/variants.rb): ios-internal = secant-testnet,
# ios-testnet = zashi-testnet, ios-appstore = secant-mainnet. This value is rarely used.
app_identifier "co.electriccoin.secant-mainnet"
team_id "RLPRR8CPQG"
